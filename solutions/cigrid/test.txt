//ok, so let's try this bs
// Integer arrays (G)
void int_arrays(int x, int* p){
  int* a = new int[x];
  int y = 1;
  a[7] = a[19+y]; 
}
/*
extern int putchar(int c);

//allowed, but change signature to test later
int putchar(int c){
    return c;
}

void print_char_consts(){
  putchar('A');
  int x =1;
  putchar('A');
  return;
}


void arith_assignments(int x, int y, int z){
  x = x + 1;
  x = x - y * 8;
  y = 12 % z / 3;
}

// If statement and if-then-else statement (S)
int if_then_else(int x, int y){
  if(x == y)
    return y;
  else 
    return x;
  
  if(x > 0){
    if(x == 3){
      int z = x;
      x = z + 1;
      
      return x;
    }
  }
  else{
    x = 7;
    return x + 1;
  }
  int z = 0;
  return x;
}


*/